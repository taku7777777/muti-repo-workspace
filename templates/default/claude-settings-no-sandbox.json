{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",

  "env": {
    "OTEL_RESOURCE_ATTRIBUTES": "workspace={{TICKET_ID}},purpose={{PURPOSE}}"
  },

  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash(git status*)",
      "Bash(git log *)",
      "Bash(git diff*)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(ls *)",
      "Bash(cat *)",
      "Bash(grep *)",
      "Bash(rg *)",
      "Bash(find *)",
      "Bash(mkdir *)"
    ],
    "ask": [
      "Bash(git push *)",
      "Bash(rm *)",
      "Bash(curl *)",
      "Bash(wget *)"
    ],
    "ask_note": "WARNING: without the OS sandbox, Bash(cat/grep/find *) can read files the Read-tool deny rules below block (e.g. `cat ~/.ssh/id_rsa`). No-sandbox mode gives tool-level guards only; it CANNOT protect secrets from shell reads. Use it only where the host is already trusted.",
    "deny": [
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Read(~/.config/gh/**)",
      "Read(~/.config/gcloud/**)",
      "Read(~/.npmrc)",
      "Edit(/{{TASK_DIR}}/agents/**)",
      "Edit(/{{TASK_DIR}}/scripts/**)",
      "Edit(/{{WORKSPACE_ROOT}}/repositories/**)",
      "Edit(/{{WORKSPACE_ROOT}}/scripts/**)",
      "Edit(/{{WORKSPACE_ROOT}}/templates/**)",
      "Edit(/{{WORKSPACE_ROOT}}/config/**)",
      "Edit(/{{WORKSPACE_ROOT}}/.claude/**)",
      "Edit(~/.claude.json)"
    ],
    "additionalDirectories": [
      "{{TASK_DIR}}",
      "{{WORKSPACE_ROOT}}/repositories"
    ]
  },

  "enabledMcpjsonServers": []
}
