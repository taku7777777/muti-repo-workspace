{
  "$schema_note": "Target repositories managed by this workspace. /setup-workspace clones every entry into repositories/<name>; /open-task creates per-task git worktrees from them. Replace the examples with your own repositories.",
  "repositories": [
    {
      "name": "phase2-demo",
      "url": "git@github.com:taku7777777/phase2-demo.git",
      "desc": "Demo Node CLI (created live in Phase 1/2 validation).",
      "type": "code"
    },
    {
      "name": "phase3-docs",
      "url": "git@github.com:taku7777777/phase3-docs.git",
      "desc": "Demo docs repo for the Phase 3 multi-repo ticket.",
      "type": "code"
    }
  ]
}
