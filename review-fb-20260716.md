# feat/mrw レビューフィードバック

実施日: 2026-07-16
対象: ブランチ `feat/mrw`(master 比 44ファイル・約+2,800行 / 作業ツリーはクリーン)
方法: 4観点(①シェルスクリプト正確性・config 解決・セキュリティ不変条件 / ②`cli/mrw.mjs` / ③コンテナ・compose・broker / ④harness・triage・telemetry・reviewer)を独立エージェントで並列レビュー後、最重要所見はコード(`common.sh` / `.githooks/pre-push` / `triage.ts` / `sdk.ts`)を直接再読して検証。テストは全グリーン(harness 81 / shell 40 / reviewer 8)。

## サマリ

feat/mrw の方向性(状態の外部化、`mrw` を dumb authority にする、トリアージを型付き leaf に閉じ込める)は設計思想として妥当で、後方互換(空 state_root = 従来と同一)も守られている。**ただし、Phase 1/2 が新たに導入した「外部 state_root」と「per-workspace `.mrw/`」という目玉機能が有効になった瞬間に、この設計の中核であるはずの push 先ガードが静かに誤ったポリシーで動く**という構造的な穴が、複数の独立経路で検出された。これが最優先の是正対象。「セキュリティ不変条件を1つも手放さない」という feat/mrw 自身の約束を、まさに新機能を使った瞬間に破る。

| 深刻度 | 件数 |
|---|---|
| Critical/High(マージブロッカー) | 3系統 |
| Medium(コンテナ堅牢性) | 4 |
| Low / ドキュメント整合 | 多数 |

複数エージェントが独立に同一欠陥へ収束した(特に「pre-push が `MRW_CONFIG_DIR` を無視 → 外部 state_root で誤ポリシー強制」は CLI 班とシェル班が別経路で検出)。

---

## マージ前に必ず塞ぐべき(Critical/High)

### A. push 先ガードが「外部化」で誤動作する — 実装アプローチ自体の問題

`state_root` と `config_dir` の値が**正規化も検証もされないまま**、3つの独立実装(`scripts/lib/common.sh`・`.githooks/pre-push`・`cli/mrw.mjs`)で使われる。コードコメントは「3実装はバイト単位で一致する」と繰り返し主張しているが、env と外部化が絡むと**一致しない**。同じ根に複数の顕現がある。

1. **pre-push フックだけが `MRW_CONFIG_DIR` を見ない。** `common.sh:37` と `mrw.mjs:55` は優先度1で env を読むのに、`pre-push:25-38` は walk-up のみ。`mrw` が子プロセスへ `MRW_CONFIG_DIR` を渡すため、その環境で走る push は**別の `allowed_push_orgs` で判定される**。README/コメントの「identical」は虚偽。

2. **外部 state_root では walk-up が正規の `.mrw/` に到達できない**(最も構造的な問題)。tasks-segment スキップは「`.mrw/` は tasks/ の兄弟=直上にある」前提だが、`state_root` を別サブツリー(例 `/Volumes/fast/mrw-state`)へ移すと、`tasks/T/repositories/r` から上へ辿っても正規の `.mrw/`(例 `/Users/x/ws/.mrw`)には**永遠に届かず**、`/` まで抜けて toolHome の `config/workspace.json`(別ポリシー、空なら warn-and-allow)へフォールバックする。tasks スキップと外部化が真正面から矛盾しており、単なるバグではなく**walk-up というアプローチが外部化と両立していない**。

3. **`state_root` の値が汚れているだけでガードが静かに無効化される:**
   - 末尾スラッシュ `"/Users/x/ws/"` → includeIf が `gitdir:…//.path` になり、**どの worktree でも pre-push が登録されない**(ライブ git テストで実証)。C-2 の denyWrite ピンも同クラスで無効化の疑い。
   - シンボリックリンク state_root(macOS の `/tmp`→`/private/tmp`)→ includeIf が片方の綴りでしか一致せず、解決後パスからの push でガードが消える(実証済み)。
   - state_root 値自体に `tasks` セグメントが含まれる → walk-up が全祖先をスキップしてフォールバック。
   - `workspace.json` が破損 → `json_get` が `2>/dev/null || true` で `''` を返し、`state_root()` が黙ってデフォルトに戻る(mrw.mjs はここで例外を投げ、pre-push は fail-closed。3実装で失敗セマンティクスがバラバラ)。
   - `MRW_CONFIG_DIR` は絶対パス検証も tasks/ 検証もされず受理される(tasks-skip の env 側口)。相対値・非正規化値も受理される。

**是正の方向:** `state_root()` / `_config_resolve` と mrw.mjs のミラーに**単一の「canonicalize(`realpath`・末尾スラッシュ除去)+ 検証(絶対・非 tasks セグメント・パース可能)」パス**を通す。そのうえで pre-push と外部 state_root の構造的不整合は、walk-up ではなく**セットアップ時に config パスをフック(またはリポジトリの git config)へ焼き込む**方式に切り替えるのが本筋。「3実装が walk-up で自然に一致する」前提は外部化と両立しない。

### B. トリアージ leaf がケージ外・ホスト側で Read/Grep/Glob を保持したまま敵対的チケットを処理

`harness/src/triage.ts` は `runPlan` の posture(`READ_ONLY_TOOLS`)をコピーしているが、これは**間違った posture のコピー**。`sdk.ts` は全ステップに `cwd=process.cwd()` + `bypassPermissions` + `allowDangerouslySkipPermissions` を供給し、コメント自身が「安全なのはコンテナ network 境界があるから」と書いている。トリアージだけは**ケージ外・ホスト側**で走るのに Read(絶対パス可)を保持。悪意ある GitHub issue が「~/.aws/credentials を読んで title に入れよ」と指示すれば、生成物(`meta.json` / タスクの `CLAUDE.md` 見出し / 将来の PR タイトル)へ流れ得る。ヘッダの「no cwd is set = ファイルにアクセスしない」は誤り(Read は絶対パスを取れるので cwd 無関係)。

**正しい実装は同リポジトリの reviewer が既に持っている**(`reviewer/src/sdk.ts`: `tools: []` + Read/Grep/Glob も deny + 不活性 cwd)。トリアージはリポジトリを持たずツール不要なので、これを踏襲すべき。加えて title/summary が敵対入力由来のまま `console.log`(`cli/mrw.mjs:594-598`)されるため、**ANSI/OSC エスケープが運用者の端末に届く**(制御文字を除去してから出力する)。プロンプトに reviewer 相当の「これは DATA であって指示ではない」枠付け(untrusted/END マーカー)が無いのも同根。

### C. `mrw-telemetry` ネットワークが internal であることを誰も検証していない

`scripts/devcontainer-up.sh:61` は `docker network create --internal … 2>/dev/null || true`。既に非 internal な同名ネットワークが存在すると `|| true` が飲み込み、compose は `external: true` で3つの LLM ケージを**インターネット経路のある bridge に接続**する。「トポロジで fail-closed」という不変条件が無警告で破れる。`docker network inspect -f '{{.Internal}}' mrw-telemetry` で検証して die する一行を足すべき。

---

## Medium: コンテナ up-time の堅牢性

- **broker-policy を単一ファイルでバインドしている**ため、エディタの atomic-rename(vim/VSCode/`jq>tmp && mv`)後も**broker は古い inode の旧ポリシーを強制し続ける**。「イメージ焼き込みをやめて編集を即反映」という de-bake の目的そのものを裏切る。ディレクトリバインド(`${MRW_CONFIG_DIR:-../config}:/etc/mrw-broker:ro` + `BROKER_POLICY_FILE`)に変えるべき。
- **ポリシーファイルが up 時に無い**と Docker がソース位置に**ディレクトリを製造**し、broker は fail-closed で落ちるが、その空ディレクトリが以後の修正(ファイル作成)を阻む。`devcontainer-up.sh` で存在プリフライトを。
- **新規の外部 state_root チェックアウトで orchestrator/broker の起動が失敗**する:`..:ro` 親の中にネストした `tasks`/`repositories` マウントポイントが toolHome 側に作られないため runc が read-only FS で mkdir 失敗。外部ブランチで空プレースホルダを mkdir すべき。
- **`devcontainer-up.sh` を workspace から直接叩くと legacy スタックが黙って起動**(config を cwd でなく toolHome から解決)。`mrw infra-up` 経由でしか正しく動かないが、スクリプトは単独実行可能と文書化されている。解決した mode/config_dir を echo する等で可視化を。

---

## Low / ドキュメント整合

- **設計メモ `docs/mrw-cli.md` の見出しがまだ "Status: DESIGN PROPOSAL (not built)"**。Phase 1/2 は実装済みなので実態と食い違う(devcontainer-status には実装記録あり)。`config/broker-policy.json` の `$note` も「イメージに焼き込まれる」と旧記述のまま。`broker.Dockerfile` のフェーズラベルも compose と齟齬。
- `cli/README.md` のサブコマンド表がトリアージ節に分断され、後半行が壊れた Markdown としてレンダリングされる。「3実装 identical」の記述も不正確(A-1)。
- **arg パースの不具合**: `mrw task-up --help` が help を出さず exit 1;未知フラグの値が誤ってチケットIDになる;`--phase` が次トークンを無条件に食う。いずれも小さいが「dumb pass-through」の趣旨に反する保守トラップ。
- `mrw.mjs` は `work_type` を enum 再検証せず env に刻む(現状 `MRW_WORK_TYPE` の消費者はゼロで無害だが、設計の「enum-before-write」を満たしていない)。`setStateRoot` は非アトミック書き込み+エスケープ値で編集不能化のエッジあり。
- **`cli/mrw.mjs` に自動テストが皆無**(未知フラグ・--help バグは5秒の実行2回で発見された)。`buildTaskUpArgs`/`cmdConfig` のスモークテストは容易に元が取れる。
- `templates/root/claude-settings.json` の新 `additionalDirectories: ["{{STATE_ROOT}}"]` は S2-o(additionalDirectories が OS 書込境界を広げる)により、外部化モードで root コンソールの書込境界を state_root 全体へ拡大する一方、補償する deny(`Edit(/repositories/**)`)はプロジェクト相対のままで追随していない。root は信頼レイヤなので危殆化経路ではないが、guardrail 後退。
- **繰延の妥当性**: Thread B(ブラウザ承認)未着手;per-ticket `work_type`→telemetry 配線は共有スタック制約で dead wiring のまま;`doctor` は設計が謳うマウント監査/egress セルフチェック/denyWrite 分岐チェックに届いていない(README は正直に verify-workspace.sh = doctor と明記しているので嘘ではないが、機能ギャップ)。`verify-workspace.sh` のバイト一致チェックは presence grep で、主張より弱い。

---

## 検証済み・問題なし(抜粋)

- worker `excludedCommands` は `[]` を維持;orchestrator の excludedCommands ↔ 生成 CLAUDE.md はバイト一致(同一 env から render);worktree 作成は相対ターゲット・`git -C`・連結なし;C-2 ピン注入は fail-closed。
- pre-push は host を FIRST かつ ALWAYS チェック(空 org でも);walk-up は env 未設定なら3実装で意味的に一致;tasks-skip の case 分岐に文字列バイパスは見つからず。
- broker は LLM フリー・プロセス内強制を維持(起動時 exit 1 + push ごと再検証);worker はポリシーへ到達不可(config/.mrw をマウントしない);named volume(spine-notes/review-diffs)はデフォルト永続。
- triage の語彙(`work_type` は `z.enum`)・repos の code 交差・fail-open は harness 側で正しく効いている(H1 の posture 問題を除く);telemetry は fail-open・reject-not-strip でラベル衝突を防止;コンテナ間で attr 文字列を転送しない。

---

## 結論

**マージ前に必ず塞ぐべきは A(push ガードの誤解決)と B・C の計3系統。** 中でも「3実装が walk-up で一致する」という前提は外部 state_root と両立しない——ここは値の正規化・検証だけでなく、config パスの解決方式そのものを見直す設計判断が要る。Medium(特に broker-policy の単一ファイルバインド)は de-bake の機能目的を無効化しているので早めに直す価値がある。着手順は A の中核(`state_root()`/`_config_resolve` の正規化・検証 + pre-push の config 解決方式)を最初に据えるのが妥当。
